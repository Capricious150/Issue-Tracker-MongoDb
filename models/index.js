// Not doing much here, just having an easy spot to export all my models

const Project = require('./Projects');
const Task = require('./Tasks');
const User = require ('./Users');

module.exports = {Project, Task, User};